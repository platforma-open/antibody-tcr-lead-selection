[project]
name = "sample-clonotypes"
version = "0.1.0"
description = "Lead selection: clonotype filtering and top-N ranking"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "polars==1.33.1",
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
